import Footer from "@/components/Footer";

export default function PolicyPage({ title, accent, intro, sections, updated = "July 30, 2026" }) {
  return <><header className="page-hero legal-hero"><div className="inner"><p className="crumb"><a href="/">Home</a> &nbsp;/&nbsp; <a href="/legal">Policies</a> &nbsp;/&nbsp; {title}</p><h1>{title.split(" ").slice(0,-1).join(" ")} <em>{accent || title.split(" ").slice(-1)}</em></h1><p>Last updated: {updated}</p></div></header><section className="services legal-page"><div className="inner legal-content"><p className="s-body legal-intro">{intro}</p>{sections.map((section) => <section key={section.title} className="legal-section"><h2 className="s-title">{section.title}</h2>{section.paragraphs?.map((text) => <p className="s-body" key={text}>{text}</p>)}{section.items && <ul>{section.items.map((item) => <li key={item}>{item}</li>)}</ul>}</section>)}<aside className="legal-review-note"><strong>Important:</strong> These website policies provide general operating terms. A signed quotation, statement of work, purchase order or project agreement may add or replace project-specific terms. Obtain independent legal advice before relying on these terms for a particular transaction.</aside><p className="legal-contact">Questions? Email <a href="mailto:hello@zameett.com">hello@zameett.com</a>.</p></div></section><Footer /></>;
}
